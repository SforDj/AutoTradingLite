0. 如何运行？
    a. 在common/config.py中检查数据库用户名，密码等是否与本地一致，如果不一致要进行修改。
    b. 运行，直接run main.py中的main函数，用pycharm运行即可

1. 如何添加股票？
    在common/config.py中的g_map中添加股票id和股票名的键值对，用英文逗号和英文引号隔开，如{"123321": "蛤蛤蛤", "213233": "呵呵呵"}
